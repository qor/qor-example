import MainLayout from '../components/MainLayout.js';

const Category = () => (
    <MainLayout>
        <p>This is category page(/men, /women etc...)</p>
    </MainLayout>
);

export default Category;
