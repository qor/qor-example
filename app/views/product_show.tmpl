<!DOCTYPE html>
<html>
<head>
{{.seoTag}}
</head>
<body>

{{ with .product }}
  <h1>Product Show Page</h1>
  <div class="product">
    <h2>{{.Name}} ({{.Code}}) - ${{.Price}}</h2>
    <div>{{.Description}}</div>
    <div>Made in {{.MadeCountry}}</div>
  </div>
{{ end }}

{{.microProduct}}

</body>
</html>
